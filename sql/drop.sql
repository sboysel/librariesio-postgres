DROP TABLE IF EXISTS projects,
                     versions,
                     tags,
                     dependencies,
                     repositories,
                     repository_dependencies,
                     projects_with_repository_fields;

